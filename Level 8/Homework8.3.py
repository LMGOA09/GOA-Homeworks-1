#4)
#<thead> means "Table header", which wraps the header rows of a table. It contains the column 
# titles (e.g., "ID", "Name", "Revenue"); If a table spans multiple printed pages, browsers 
#often repeat this header at the top of each page;

#<tbody> means "Table body", which wraps the core content/data rows of the table. This is where 
#the main bulk of your numbers or records live.

#<tfoot> means "Table footer", which wraps the summary or footer rows at the bottom of the table. 
#It is perfect for displaying calculations like totals, averages, or "Page 1 of 3".