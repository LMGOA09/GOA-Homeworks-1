//#1)
/* An Array is a special type of variable in programming used to store multiple values in a single ordered list Instead of 
creating 5 separate variables for 5 items, you can group them together in one array. */

//#2)
//length: A property that returns the total count of elements inside the array (e.g. [10, 20, 30].length -> 3);
//push: Adds one or more elements to the end of an array (e.g. arr.push('Mango"));
//pop: Removes the last element from an array (e.g. arr.pop());
//unshift: Adds one or more elements to the beginning of an array (e.g. arr.unshift("Strawberry"));
//shift: Removes the first element from an array (e.g. arr.shift())